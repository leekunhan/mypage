---
const base = import.meta.env.BASE_URL; // '/mypage'
const pathname = Astro.url.pathname;

function isActive(path: string): boolean {
  if (path === '/') return pathname === base || pathname === `${base}/`;
  return pathname.startsWith(`${base}${path}`);
}

const navItems = [
  { href: `${base}/`, path: '/', label: '首頁' },
  { href: `${base}/chatbot`, path: '/chatbot', label: 'Chatbot' },
  {
    href: `${base}/travel`, path: '/travel',
    label: '旅遊',
    children: [
      { href: `${base}/travel/okinawa`, path: '/travel/okinawa', label: '🏝️ 沖繩' },
      { href: `${base}/travel/okinawa/itinerary`, path: '/travel/okinawa/itinerary', label: '　五日行程' },
      { href: `${base}/travel/okinawa/food`, path: '/travel/okinawa/food', label: '　美食' },
      { href: `${base}/travel/okinawa/location`, path: '/travel/okinawa/location', label: '　景點' },
      { href: `${base}/travel/okinawa/home`, path: '/travel/okinawa/home', label: '　住宿' },
    ]
  },
];
---

<nav class="main-nav" aria-label="Main navigation">
  {navItems.map((item) => (
    item.children ? (
      <div class="nav-dropdown">
        <a href={item.href} class:list={['nav-link', { active: isActive(item.path) }]}>
          {item.label} ▾
        </a>
        <div class="nav-dropdown-content">
          {item.children.map((child) => (
            <a href={child.href} class:list={['nav-dropdown-link', { active: isActive(child.path) }]}>
              {child.label}
            </a>
          ))}
        </div>
      </div>
    ) : (
      <a href={item.href} class:list={['nav-link', { active: isActive(item.path) }]}>
        {item.label}
      </a>
    )
  ))}
</nav>
